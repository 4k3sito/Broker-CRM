# Secretos
.env
.env.local

# Entornos / dependencias
.venv/
node_modules/
__pycache__/
*.pyc

# Herramientas locales (viven fuera del repo)
.claude/
.agents/
AGENTS.md
graphify-out/

# Scrapers: salida de corridas y fixtures de terceros — nunca al repo público
scrapers/data/
scrapers/.fixtures/
scrapers/logs/
scrapers/*.log
scrapers/.cron.lock

# Restos de builds/experimentos
.next/
cache/
error_logs/
*.dc.html

# Credenciales de proxy en el servidor — nunca al repo
scrapers/.env

# Logs de corridas sueltas en la raíz
*.log
